# Verification report

검증일: 2026-09-06. 프로젝트: TOUCHLINE ELEVEN.

## 자동 검증 결과

- `npm test`: **60개 통과**, 5개 테스트 파일.
- `npm run build`: **성공**, TypeScript 오류 없음.
- Node.js 24에서 로컬 검증. Netlify/GitHub Actions 설정은 Node.js 22.
- 게임 앱은 백엔드·API 키·외부 모델/음원 없이 빌드 가능.

| 범위 | 검증 내용 | 결과 |
| --- | --- | --- |
| 규칙 순수 함수 | 공 전체 골라인 통과, 높이·폭, 고속 공 교차점, 진영별 득점, 스로인·코너·골킥 | 통과 |
| 오프사이드 | 두 번째 수비수·공·상대 진영·동일 선·진영 교체 | 통과 |
| 입력 좌표 | 4개 카메라 전방 벡터의 위/오른쪽, 대각선 정규화 | 통과 |
| 팀 데이터 | 22명 고유 ID, 팀별 11명, 골키퍼 2명 | 통과 |
| AI·통계 | 포메이션 목표, 패스 경로, 슈팅 점수, 선수 전환, 난이도, 점유율·성공률 | 통과 |
| 경기 상태 | 득점 중복 방지, 재개 팀, 일시 정지 시간, 전후반·회복, 무승부 종료, 재시작 초기화 | 통과 |
| 세트피스·징계 | 단일 코너 재개, 간접 재개 직접 득점 불가, 페널티킥, 두 번째 경고·직접 퇴장 | 통과 |
| 오프사이드 관여 | 패스 순간 위치 기록, 실제 수신 시 선언, 상대 소유 시 초기화 | 통과 |
| Rapier 실제 물리 | 전후반 완주, 선수 캡슐과 공 충돌, 드리블, 카메라별 이동, 슛 속도·재획득 제한 | 통과 |

실제 Rapier 시뮬레이션은 양 팀 22개 kinematic 몸체와 동적 공·바닥·골대 충돌체로 실행했습니다. 최종 기본 시드에서 1분씩 두 하프를 진행한 결과: 점수 0:6, 패스 7:24, 성공 패스 7:22, 슈팅 0:10. 사용자가 움직이지 않는 시나리오이므로 난이도 균형이나 플레이 품질의 수동 평가를 대체하지 않습니다.

## 브라우저에서 확인한 항목

- 메인 메뉴·팀/난이도/시간/포메이션/전술 선택 UI 렌더링.
- 경기 시간 선택을 1분으로 변경.
- 설정 대화상자, 그래픽 품질 변경, 음소거 체크, 닫기, 조작 방법 표 열기.
- WebGL 미지원 안내와 실행 버튼 비활성화.

## 브라우저 검증 제한

검증용 Chrome의 그래픽 시스템이 `GL_RENDERER=Disabled`로 WebGL 컨텍스트를 제공하지 않았습니다. 따라서 **3D 화면의 시각적 검수, GPU 브라우저 내 실플레이, GPU FPS, 실제 게임패드, 모바일 뷰포트 수동 검증은 미완료**입니다. 초기 WebGL 생성 오류가 관찰되어 지원 여부 검사와 사용자 안내를 추가했습니다. GPU 브라우저 콘솔 오류가 없다고 주장할 수 없습니다.

## 알려진 빌드/런타임 경고

- Rapier의 번들 내 WASM 때문에 큰 청크 경고가 남습니다. 빌드 실패는 아닙니다.
- Rapier 0.19.2의 초기화 내부에서 deprecated parameters 경고가 출력됩니다. 물리 테스트는 통과합니다.

## GPU 브라우저에서 이어서 점검할 체크리스트

- [ ] 22명 선수·공·골대·라인·관중이 정상 표시되는지
- [ ] 방향키가 세 카메라에서 화면 방향과 일치하는지
- [ ] 패스·스루패스·크로스·충전 슛·태클·Tab 전환의 실플레이 감각
- [ ] 공 가로채기·골키퍼 선방·득점과 킥오프 연출
- [ ] 경기 중 모든 세트피스·오프사이드·카드 처리
- [ ] 전반 종료·진영 교체·후반 종료·결과 화면
- [ ] 일시 정지에서 설정·조작 안내·재시작·메뉴 복귀
- [ ] 1920×1080 낮음/중간/높음 FPS
- [ ] 실제 패드의 버튼·스틱·연결 해제
- [ ] 외부 GLB와 사용자 텍스처·음원 교체

## 디자인 업데이트 검증

선수 렌더링을 AthleticRig로 교체하고 중계 카메라·잔디·라인·조명·HUD를 개선한 후 기존 자동 테스트 46개가 통과했습니다. 경기 규칙과 입력 변환 코드는 유지했습니다. 프로덕션 빌드가 성공했으며 WebGL/GPU 시각 검수 제약은 그대로 남아 있습니다.

## 크기와 입력 업데이트 검증

실제 Rapier 테스트도 새 반지름 0.11m를 사용하도록 변경했습니다. 공 수신 시 홈/원정/GK 자동 전환 및 상대·오프사이드 수신 제외를 검증했습니다. S/D/A 동작 매핑, 동작 키와 이동 키의 분리, 방향키 이동 테스트를 추가해 총 53개가 통과했습니다. 카메라 거리·높이와 하단 안내의 GPU 브라우저 시각 검수는 기존 환경 제약으로 미완료입니다.

## 근접 전환 및 인체 메시 업데이트 검증

근접 자동 전환·작은 거리 차이 유지·오프사이드/퇴장 제외·상대 소유 시 유지·먼 공 제외·Space/S 매핑·인체 메시 표면 방향을 검증하는 테스트 7개를 추가했습니다. 총 60개 통과. 실제 사람과 같은 시각적 품질은 GPU 브라우저에서 추가 검수가 필요합니다.
